{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-surfaces",
  "description": "Allow RouteX floating and tray menu windows to receive desktop events and use desktop IPC.",
  "local": true,
  "windows": ["floating", "traymenu"],
  "permissions": ["core:default", "allow-desktop-invoke"]
}
